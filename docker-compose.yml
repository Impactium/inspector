name: inspector

services:
  api:
    build:
      context: .
    container_name: ${COMPOSE_PROJECT_NAME}-api
    command: pnpm run start:prod
    env_file: .env
    ports:
      - 1488:1488
    restart: always
    networks:
      - default

networks:
  default:
    driver: bridge
